Algoritmo "volumelata"

 // Volume de uma lata de óleo
 // v = pi * r² * h

Var

 r, h , v : real

Inicio

 escreva("Insira o valor do Raio (r):")
 leia(r)
 escreva("Insira o valor da altura (h):")
 leia(h)
 
 v <- pi * exp(r,2) * h
 
 escreva("Volume do cilindro = ", v:6:2,"cm³")

Fimalgoritmo